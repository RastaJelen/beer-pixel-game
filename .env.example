# Database Configuration
DATABASE_URL=postgresql://username:password@hostname:port/database_name

# Application Configuration
NODE_ENV=production
PORT=5000

# Optional: For local development
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=beer_pixel_game